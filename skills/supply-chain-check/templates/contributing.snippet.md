## Security setup (required)

Before running `npm install` for the first time, install Aikido Safe Chain:
https://github.com/AikidoSec/safe-chain#installation

Pin the version you install — do not track `latest`. Bump it deliberately during security review windows.
